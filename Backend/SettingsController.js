class Settings {
    constructor(notifications, sound, banner, batch) {
        this.notifications = notifications;
        this.sound = sound;
        this.banner = banner;
        this.batch = batch;
    }
}
